rjqueue --setup

  copy over 
    config/jobs.yml
    config/initializers/jobs.rb
    migration
    make app/jobs
    think about verify with sqlite
  
* on start up check for the jobs table and fail before daemonizing
* report missing job requests better (check in the schedule method) e.g. not setting details on the job
* generate monit configuration, from jobs.yml
* create worker processes
